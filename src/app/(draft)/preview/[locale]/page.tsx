// Preview mirror — uses drafts perspective via parent layout's enableDraftFetch().
// Force-dynamic is inherited from the parent (draft) layout.
export { default } from "@/src/app/(content)/[locale]/page";
